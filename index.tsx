import { registerRootComponent } from 'expo';
import { Text } from 'react-native';
import 'react-native-screens';

function App() {
  return <Text>OliveOps Screens Import OK</Text>;
}

registerRootComponent(App);
